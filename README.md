This code shows how to set up simple state management in React that can be applied for most use cases without
needing to resort to Redux or MobX, it shows at its core the provider consumer relationship.